$ "bin/mocha --parallel --jobs 12 test/integration/**/*.spec.js"

# -- parallel command for using mocha v8.0.0 for execute paralel test running
# --jobs 12 option to specify exactly how many worker processes Mocha will potentially use
# test/integration/* path directory test

 
